name: Sync to Hugging Face hub

on:
  push:
    branches: [main]
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true # Ensures Large File Storage is pulled correctly
          
      - name: Push to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        # Replace 'your-username' and 'your-repo-name' with your actual HF details
        run: git push https://anthony01:$HF_TOKEN@huggingface.co/spaces/anthony01/LumiSign/tree/main